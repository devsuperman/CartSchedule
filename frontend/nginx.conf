server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: rotas do React Router (/admin/*, /historico, ...) caem no index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
